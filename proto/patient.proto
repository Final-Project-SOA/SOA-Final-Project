syntax = "proto3";

package patient;

service PatientService {
  rpc GetPatients (Empty) returns (PatientList);
}

message Empty {}

message Patient {
  int32 id = 1;
  string name = 2;
}

message PatientList {
  repeated Patient patients = 1;
}